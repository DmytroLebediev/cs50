#include <stdio.h>
#include <cs50.h>
#include <math.h>

int main()
{
    // get input from user and save it to variable
    
    // get sum of card digits at even indexes (0 based)
    // get sum of card digits at odd indexes (0 based)
    
    // display result
    printf("Sum of digits at even indexes:\t%i\n", sum_even);
    printf("Sum of digits at odd indexes:\t%i\n", sum_odd);
    printf("Total:\t%i\n", sum_even + sum_odd);
    
    return 0;
}
